version: '3.8'

services:
  automationlist:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=4321
      - HOST=0.0.0.0
    restart: unless-stopped

