---
interface Props {
  title?: string;
  steps?: Array<{
    title: string;
    description: string;
  }>;
}

const {
  title = "How It Works",
  steps = [
    {
      title: "1. Search",
      description: "Find vendors by category, technology or country.",
    },
    {
      title: "2. Compare",
      description: "Review profiles, capabilities and technologies.",
    },
    {
      title: "3. Contact",
      description: "Reach out directly — no middleman.",
    },
  ],
} = Astro.props;
---

<section class="fade-in-up container-px mx-auto max-w-7xl px-4 py-16 md:py-20 border-t border-gray-200 mt-8 pt-8">
  <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-6 md:mb-10 text-center">{title}</h2>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-10 text-center">
    {steps.map((step) => (
      <div>
        <h3 class="text-lg font-semibold text-gray-900 mb-2">{step.title}</h3>
        <p class="text-gray-600 text-sm">{step.description}</p>
      </div>
    ))}
  </div>
</section>

